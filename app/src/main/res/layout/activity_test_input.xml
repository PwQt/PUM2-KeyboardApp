<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:focusableInTouchMode="true"
    tools:context="pl.polsl.aei.pum2.keyboard.TestInputActivity"
    tools:showIn="@layout/activity_test_input">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="200dp"
        android:id="@+id/sendTextButton"
        android:text="@string/send"/>

    <EditText
        android:layout_width="300dp"
        android:layout_height="100dp"
        android:layout_above="@+id/sendTextButton"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="128dp"
        android:id="@+id/sendEditText"
        android:inputType="text"
        />

    <Button
        android:id="@+id/testInputReturnButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="28dp"
        android:layout_marginStart="24dp"
        android:text="@string/returnButtonText"/>

</RelativeLayout>
